import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["images.ajkerpatrika.com", "www.washingtonpost.com"], // Add the domain hosting your images
  },
};
export default nextConfig;
